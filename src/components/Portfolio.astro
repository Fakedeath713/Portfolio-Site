---
// src/components/Portfolio.astro
const { items = [], filters = [] } = Astro.props;
---

<section class="min-h-screen bg-white/25 backdrop-blur-[2px] py-28 px-4 sm:px-8">
  <div class="max-w-[1600px] mx-auto">
    <h2 class="font-title text-4xl sm:text-5xl lg:text-6xl font-black text-center text-gray-800 mb-16 relative inline-block left-1/2 -translate-x-1/2">
      Portfolio
      <div class="absolute -bottom-3 left-1/2 -translate-x-1/2 w-36 h-1.5 bg-gradient-to-r from-black to-gray rounded-full"></div>
    </h2>
    
    <!-- Filters -->
    <div class="flex flex-wrap justify-center gap-4 mb-16">
      <button 
        class="filter-btn px-8 py-3 border-2 border-gray-300 bg-white text-gray-600 text-base font-semibold rounded-full cursor-pointer transition-all duration-300 uppercase tracking-wide hover:border-purple-500 hover:text-purple-600 hover:-translate-y-0.5 hover:shadow-[0_10px_20px_rgba(102,126,234,0.2)] active" 
        data-filter="all"
      >
        All
      </button>
      {filters.map(filter => (
        <button 
          class="filter-btn px-8 py-3 border-2 border-gray-300 bg-white text-gray-600 text-base font-semibold rounded-full cursor-pointer transition-all duration-300 uppercase tracking-wide hover:border-purple-500 hover:text-purple-600 hover:-translate-y-0.5 hover:shadow-[0_10px_20px_rgba(102,126,234,0.2)]" 
          data-filter={filter.name}
        >
          {filter.label}
        </button>
      ))}
    </div>
    
    <!-- Portfolio Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
      {items.map((item, index) => (
        <div 
          class="portfolio-item opacity-0 scale-90"
          data-category={item.category}
          data-image={item.image}
          data-title={item.title}
          style={`animation-delay: ${(index % 12) * 0.05}s;`}
        >
          <div class="group relative rounded-3xl overflow-hidden bg-white shadow-[0_10px_30px_rgba(0,0,0,0.1)] transition-all duration-400 hover:-translate-y-2 hover:shadow-[0_20px_50px_rgba(0,0,0,0.2)] cursor-pointer">
            <div class="relative w-full pb-[120%] overflow-hidden">
              <img 
                src={item.image} 
                alt={item.title} 
                loading="lazy"
                class="absolute inset-0 w-full h-full object-cover transition-transform duration-400 group-hover:scale-110"
              />
              
              <!-- Overlay -->
              <div class="absolute inset-0 bg-gradient-to-t from-white/95 via-transparent to-transparent flex items-end p-8 opacity-0 transition-opacity duration-400 group-hover:opacity-100">
                <div class="transform translate-y-5 transition-transform duration-400 group-hover:translate-y-0">
                  <h3 class="text-2xl font-bold text-gray-800">{item.title}</h3>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const portfolioItems = document.querySelectorAll('.portfolio-item');
    
    // Filter functionality
    filterBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const filter = btn.getAttribute('data-filter');
        
        // Update active button
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        
        // Filter items
        portfolioItems.forEach(item => {
          const category = item.getAttribute('data-category');
          
          if (filter === 'all' || category === filter) {
            item.style.display = 'block';
            setTimeout(() => {
              item.classList.add('show');
            }, 10);
          } else {
            item.classList.remove('show');
            setTimeout(() => {
              item.style.display = 'none';
            }, 300);
          }
        });
      });
    });
    
    // Initialize all items as visible
    portfolioItems.forEach(item => {
      item.classList.add('show');
    });
    
    // Open lightbox on portfolio item click
    portfolioItems.forEach((item) => {
      item.addEventListener('click', () => {
        // Get ONLY currently visible items at the moment of click
        const currentVisibleItems = Array.from(portfolioItems).filter(el => 
          el.style.display !== 'none' && window.getComputedStyle(el).display !== 'none'
        );
        
        const clickedIndex = currentVisibleItems.indexOf(item);
        
        // Prepare items array for lightbox
        const itemsForLightbox = currentVisibleItems.map(el => ({
          image: el.getAttribute('data-image'),
          title: el.getAttribute('data-title')
        }));
        
        // Dispatch custom event to open global lightbox
        document.dispatchEvent(new CustomEvent('openLightbox', {
          detail: {
            items: itemsForLightbox,
            index: clickedIndex
          }
        }));
      });
    });
  });
</script>

<style>
  .filter-btn.active {
    @apply bg-gradient-to-br from-purple-500 to-purple-700 text-white border-transparent shadow-[0_10px_30px_rgba(102,126,234,0.3)];
  }
  
  .portfolio-item.show {
    animation: fadeInScale 0.6s ease forwards;
  }
  
  @keyframes fadeInScale {
    from {
      opacity: 0;
      transform: scale(0.8) translateY(30px);
    }
    to {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }
</style>